# Миграция на внутреннюю базу данных - Инструкция по отключению API

## Дата обновления
Документ обновлен после полной миграции на локальную БД и удаления экранов авторизации.

## Текущий статус миграции

### ✅ Полностью переведено на локальную БД

**Все операции записи и чтения данных пользователя работают через локальную БД:**

1. **Управление книгами**
   - ✅ Загрузка списка книг - из локальной БД (fallback на сервер только при первом запуске)
   - ✅ Поиск книг - полностью через локальную БД
   - ✅ Изменение статуса книги - локальная БД
   - ✅ Обновление даты добавления - локальная БД
   - ✅ Сохранение заметок - локальная БД
   - ✅ Сохранение рейтингов - локальная БД
   - ✅ Обновление лайков - локальная БД
   - ✅ Удаление заметок - локальная БД
   - ✅ Удаление рейтингов - локальная БД
   - ✅ Загрузка категорий - из локальной БД (fallback на сервер только при первом запуске)

2. **Управление целями**
   - ✅ Создание цели - локальная БД
   - ✅ Обновление цели - локальная БД
   - ✅ Удаление цели - локальная БД
   - ✅ Загрузка элементов цели - из локальной БД (fallback на сервер только при первом запуске)
   - ✅ Добавление элемента цели - локальная БД
   - ✅ Удаление элемента цели - локальная БД

3. **Авторизация и профиль**
   - ✅ Проверка авторизации (`checkAuth`) - работает в фоне, не блокирует доступ
   - ✅ Загрузка профиля - из локальной БД
   - ✅ Создание гостевого пользователя - локальная БД (для новых пользователей без токена)
   - ✅ Загрузка рейтингов, лайков, заметок, целей - из локальной БД при запуске

4. **Статистика**
   - ✅ Статистика книг - подсчитывается локально
   - ✅ Статистика страниц - подсчитывается локально

5. **Кастомные книги**
   - ✅ Создание - локальная БД
   - ✅ Обновление - локальная БД
   - ✅ Удаление - локальная БД

## Структура локальной базы данных

Все данные пользователя сохраняются в SQLite базу данных (`bookdesk.db`):

- **board_data** - кэш данных досок (книги с фильтрами и сортировкой)
- **book_ratings** - рейтинги книг пользователя
- **book_votes** - количество лайков книг
- **user_votes** - лайки пользователя (массив)
- **book_dates** - даты добавления книг и их статусы
- **book_notes** - заметки пользователя к книгам
- **goal_items** - элементы целей (прочитанные страницы)
- **user_goal** - цели пользователя (количество страниц, тип)
- **user_profile** - профиль пользователя (ID, email, дата регистрации)
- **categories** - категории книг (кэш)

## Что можно отключить через 10 дней

### ✅ Можно полностью удалить (не используются в UI)

**Экраны авторизации и регистрации удалены из навигации:**
- ❌ `AuthService.signIn` - **НЕ ИСПОЛЬЗУЕТСЯ** (экраны SignIn/SignUp не используются в навигации)
- ❌ `AuthService.signUp` - **НЕ ИСПОЛЬЗУЕТСЯ** (экраны SignIn/SignUp не используются в навигации)
- ❌ `src/screens/Auth/SignIn/` - экраны можно удалить
- ❌ `src/screens/Auth/SignUp/` - экраны можно удалить
- ❌ `src/redux/actions/authActions.ts` - методы `signIn` и `signUp` можно удалить

**Эти методы и экраны остались в коде, но не вызываются нигде в приложении.**

### ⚠️ Можно сделать опциональными (используются только как fallback)

1. **DataService.getBookList**
   - Используется только если данных нет в локальной БД (первый запуск)
   - Можно удалить - приложение будет работать с пустым списком книг для новых пользователей

2. **DataService.getCategories**
   - Используется только если категорий нет в локальной БД (первый запуск)
   - Можно удалить - приложение будет работать без категорий для новых пользователей

3. **GoalsService.getGoalItems**
   - Используется только если элементов цели нет в локальной БД (первый запуск)
   - Можно удалить - приложение будет работать с пустыми целями для новых пользователей

4. **AuthService.checkAuth**
   - Используется только для валидации токена (если токен есть)
   - Работает в фоне, не блокирует доступ
   - Можно удалить - приложение будет работать без валидации токена

### ❌ Опциональные (можно оставить или удалить)

1. **AppService.supportApp** - опциональный, используется для поддержки приложения
   - Можно удалить, если не нужна функциональность поддержки

## Инструкция по полному удалению внешнего API

### Шаг 1: Удаление неиспользуемых сервисов

#### 1.1. Удалить AuthService (полностью не используется)

**Файл:** `src/http/services/auth.ts`

```typescript
// Удалить весь файл или оставить пустую заглушку:
const AuthService = () => ({
  // Все методы удалены - не используются
});

export default AuthService;
```

**Файлы для обновления:**
- `src/redux/actions/authActions.ts` - удалить все вызовы `AuthService().signIn()`, `AuthService().signUp()`, `AuthService().checkAuth()`
- Удалить импорт `import AuthService from '~http/services/auth';`

#### 1.2. Удалить DataService (опционально, используется только как fallback)

**Файл:** `src/http/services/books.ts`

```typescript
// Удалить весь файл или оставить пустую заглушку:
const DataService = () => ({
  // Все методы удалены - не используются
});

export default DataService;
```

**Файлы для обновления:**
- `src/redux/actions/booksActions.ts` - удалить вызовы `DataService().getBookList()` и `DataService().getCategories()`
- Удалить импорт `import DataService from '~http/services/books';`
- Обновить логику `loadBookList` и `loadCategories` - убрать fallback на сервер

#### 1.3. Удалить GoalsService (опционально, используется только как fallback)

**Файл:** `src/http/services/goals.ts`

```typescript
// Удалить весь файл или оставить пустую заглушку:
const GoalsService = () => ({
  // Все методы удалены - не используются
});

export default GoalsService;
```

**Файлы для обновления:**
- `src/redux/actions/goalsActions.ts` - удалить вызов `GoalsService().getGoalItems()`
- Удалить импорт `import GoalsService from '~http/services/goals';`
- Обновить логику `getGoalItems` - убрать fallback на сервер

#### 1.4. Удалить AppService (опционально)

**Файл:** `src/http/services/app.ts`

```typescript
// Удалить весь файл или оставить пустую заглушку:
const AppService = () => ({
  // Все методы удалены - не используются
});

export default AppService;
```

**Файлы для обновления:**
- `src/redux/actions/appActions.ts` - удалить вызов `AppService().supportApp()`
- Удалить импорт `import AppService from '~http/services/app';`

### Шаг 2: Обновление логики загрузки данных

#### 2.1. Обновить `loadBookList` в `src/redux/actions/booksActions.ts`

```typescript
// Убрать fallback на сервер, если данных нет в кэше - возвращать пустой массив
if (!forceRefresh) {
  const cachedData = await loadBoardData(...);
  if (cachedData) {
    return cachedData;
  }
  // Если данных нет - возвращаем пустой массив (не загружаем с сервера)
  return {
    boardType,
    data: [],
    totalItems: 0,
    // ...
  };
}
```

#### 2.2. Обновить `loadCategories` в `src/redux/actions/booksActions.ts`

```typescript
// Убрать fallback на сервер, если категорий нет - возвращать пустой массив
const cachedCategories = await loadCategoriesFromDB(language);
if (cachedCategories.length > 0) {
  return cachedCategories;
}
// Если категорий нет - возвращаем пустой массив
return [];
```

#### 2.3. Обновить `getGoalItems` в `src/redux/actions/goalsActions.ts`

```typescript
// Убрать fallback на сервер, если элементов нет - возвращать пустой массив
const localItems = await loadGoalItems();
if (localItems.length > 0) {
  return localItems;
}
// Если элементов нет - возвращаем пустой массив
return [];
```

#### 2.4. Обновить `checkAuth` в `src/redux/actions/authActions.ts`

```typescript
// Убрать валидацию токена на сервере
// Если есть токен, но нет профиля в локальной БД - просто создаем гостевого пользователя
if (token) {
  let profile = await loadProfile();
  if (!profile) {
    // Создаем гостевого пользователя вместо загрузки с сервера
    await saveGuestProfile();
    profile = await loadProfile();
  }
  // Загружаем данные из локальной БД
  // ...
}
```

### Шаг 3: Удаление конфигурации API

#### 3.1. Обновить `src/config/api.ts`

```typescript
// Можно удалить функцию getApiUrl или оставить заглушку
export const getApiUrl = async (): Promise<string> => {
  // Возвращаем пустую строку или удаляем функцию
  return '';
};
```

#### 3.2. Обновить `src/http/http.ts`

```typescript
// Можно удалить весь файл или оставить минимальную заглушку
// Axios больше не нужен для работы с API
```

### Шаг 4: Удаление зависимостей (опционально)

**Файл:** `package.json`

Можно удалить (если больше не используются):
- `axios` - если полностью удален HTTP клиент
- `@react-native-community/netinfo` - уже удален (проверка интернета)

### Шаг 5: Очистка кода

#### 5.1. Удалить неиспользуемые импорты

Проверить и удалить во всех файлах:
- `import AuthService from '~http/services/auth';`
- `import DataService from '~http/services/books';`
- `import GoalsService from '~http/services/goals';`
- `import AppService from '~http/services/app';`
- `import { getApiUrl } from '~config/api';`
- `import http from '~http/http';`

#### 5.2. Удалить неиспользуемые экраны

- `src/screens/Auth/SignIn/` - экраны авторизации (не используются в навигации)
- `src/screens/Auth/SignUp/` - экраны регистрации (не используются в навигации)

**Файлы для удаления:**
- `src/screens/Auth/SignIn/index.tsx`
- `src/screens/Auth/SignIn/styles.ts` (если есть)
- `src/screens/Auth/SignUp/index.tsx`
- `src/screens/Auth/SignUp/styles.ts` (если есть)

#### 5.3. Удалить неиспользуемые Redux actions

**Файл:** `src/redux/actions/authActions.ts`

Удалить методы:
- `signIn` - не используется (экраны удалены)
- `signUp` - не используется (экраны удалены)
- `signInFailed` - не используется
- `setSignInError` - не используется
- `setSignUpError` - не используется

**Файл:** `src/redux/reducers/authReducer.ts`

Удалить обработчики:
- `signIn.pending`, `signIn.fulfilled`, `signIn.rejected`
- `signUp.pending`, `signUp.fulfilled`, `signUp.rejected`
- `setSignInError`, `setSignUpError`, `signInFailed`

**Файл:** `src/redux/selectors/auth.ts`

Удалить селекторы (если не используются):
- `getSignInLoadingDataStatus`
- `getSignInErrors`
- `getIsSignedUp`
- `getSignUpLoadingDataStatus`
- `getSignUpErrors`

#### 5.4. Удалить маршруты

**Файл:** `src/constants/routes.ts`

```typescript
// Удалить:
export const SIGN_IN_ROUTE = 'SignIn';
export const SIGN_UP_ROUTE = 'SignUp';
```

#### 5.5. Удалить переводы (опционально)

**Файлы:**
- `src/translations/locales/ru/auth.json` - можно удалить или оставить
- `src/translations/locales/en/auth.json` - можно удалить или оставить

## Проверка перед отключением

Перед полным отключением API рекомендуется:

1. ✅ Убедиться, что все пользователи зашли в приложение хотя бы раз (для синхронизации данных)
2. ✅ Проверить, что все операции работают через локальную БД
3. ✅ Протестировать работу приложения в офлайн режиме
4. ✅ Убедиться, что новые пользователи могут работать с приложением без API
5. ✅ Проверить, что рейтинги, лайки, заметки сохраняются и загружаются из локальной БД

## Итог

**Приложение полностью готово к работе без внешнего API.**

- ✅ Все операции записи работают через локальную БД
- ✅ Все операции чтения работают через локальную БД
- ✅ Экраны авторизации и регистрации удалены из навигации
- ✅ Новые пользователи автоматически создаются как гостевые
- ✅ Пользователи с токеном работают с локальной БД
- ✅ При перезапуске все данные загружаются из локальной БД
- ✅ Рейтинги и лайки сохраняются и загружаются из локальной БД

## Чеклист для полного удаления API через 10 дней

### 1. Удаление HTTP сервисов
- [ ] Удалить `src/http/services/auth.ts` (или оставить пустую заглушку)
- [ ] Удалить `src/http/services/books.ts` (или оставить пустую заглушку)
- [ ] Удалить `src/http/services/goals.ts` (или оставить пустую заглушку)
- [ ] Удалить `src/http/services/app.ts` (или оставить пустую заглушку)

### 2. Обновление Redux actions
- [ ] Удалить методы `signIn`, `signUp` из `src/redux/actions/authActions.ts`
- [ ] Убрать вызовы `AuthService().checkAuth()` из `checkAuth` action
- [ ] Убрать fallback на сервер в `loadBookList` (вернуть пустой массив если нет данных)
- [ ] Убрать fallback на сервер в `loadCategories` (вернуть пустой массив если нет данных)
- [ ] Убрать fallback на сервер в `getGoalItems` (вернуть пустой массив если нет данных)

### 3. Удаление экранов и компонентов
- [ ] Удалить `src/screens/Auth/SignIn/`
- [ ] Удалить `src/screens/Auth/SignUp/`
- [ ] Удалить маршруты `SIGN_IN_ROUTE` и `SIGN_UP_ROUTE` из `src/constants/routes.ts`

### 4. Очистка Redux
- [ ] Удалить обработчики `signIn` и `signUp` из `src/redux/reducers/authReducer.ts`
- [ ] Удалить неиспользуемые селекторы из `src/redux/selectors/auth.ts`
- [ ] Удалить неиспользуемые actions из `src/redux/actions/authActions.ts`

### 5. Удаление конфигурации
- [ ] Обновить `src/config/api.ts` (удалить или оставить заглушку)
- [ ] Удалить `src/http/http.ts` (или оставить заглушку)

### 6. Удаление зависимостей (опционально)
- [ ] Удалить `axios` из `package.json` (если больше не используется)
- [ ] Удалить `@react-native-community/netinfo` из `package.json` (уже не используется)

### 7. Тестирование
- [ ] Протестировать работу приложения без API
- [ ] Проверить создание гостевого пользователя
- [ ] Проверить сохранение и загрузку рейтингов
- [ ] Проверить сохранение и загрузку лайков
- [ ] Проверить сохранение и загрузку заметок
- [ ] Проверить работу с целями
- [ ] Проверить работу с кастомными книгами

**После выполнения всех пунктов приложение будет работать полностью офлайн, используя только локальную SQLite базу данных.**
